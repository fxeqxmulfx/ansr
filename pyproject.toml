[project]
name = "ansr"
version = "0.3.0"
description = "Across Neighbourhood Search with Restarts (ANSR)"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE.txt"]
requires-python = ">=3.11"
dependencies = ["numpy>=2.0"]

[project.optional-dependencies]
tests = ["pytest>=8.4.0"]
